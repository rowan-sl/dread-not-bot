
#everything must be inside the class, because the class will be serialized.
class ExampleCogg:
    #put import statements here
    import re
    def __init__(self):
        pass
    
    def check(self, author, content, chat_queue, logger):
        self.re.search(r'a', "ae")
        chat_queue.put("testing 1 2 3")
        return True